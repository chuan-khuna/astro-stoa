---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<div class="px-[clamp(1rem,5vw,3rem)] pt-10 pb-5">
  <div class="border-t border-dotted border-rule"></div>
  <p class="text-center font-body tracking-[0.14em] uppercase text-muted pt-3">
    {title}
  </p>
</div>
